# CS Role Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar role CS (Customer Success) com acesso somente-leitura a Vendas.

**Architecture:** Nova migration SQL adiciona CS ao enum e insere permissões. Dois arquivos TypeScript recebem edições cirúrgicas para incluir CS nos tipos e hooks.

**Tech Stack:** React 18, TypeScript, Supabase (PostgreSQL), shadcn/ui

---

### Task 1: Migration SQL — enum + permissões

**Files:**
- Create: `supabase/migrations/20260409000000_add-cs-role.sql`

- [ ] **Step 1: Criar migration**

```sql
-- Adiciona CS ao enum app_role
ALTER TYPE app_role ADD VALUE IF NOT EXISTS 'CS';

-- Insere permissões do role CS
INSERT INTO role_permissions (role, resource_key, can_view, can_edit)
VALUES
  ('CS', 'route:vendas',            true,  false),
  ('CS', 'section:vendas:exportar', true,  false),
  ('CS', 'section:vendas:criar',    false, false),
  ('CS', 'section:vendas:editar',   false, false)
ON CONFLICT (role, resource_key) DO UPDATE
  SET can_view = EXCLUDED.can_view,
      can_edit = EXCLUDED.can_edit;
```

- [ ] **Step 2: Commit**

```bash
git add supabase/migrations/20260409000000_add-cs-role.sql
git commit -m "feat: add CS role with vendas read-only permissions"
```

---

### Task 2: Atualizar tipos TypeScript

**Files:**
- Modify: `src/types/roles.ts`

- [ ] **Step 1: Adicionar CS ao AppRole e demais maps**

```typescript
export type AppRole = 'MASTER' | 'DIRETORIA' | 'GESTOR_COMERCIAL' | 'SDR' | 'CLOSER' | 'SOCIAL_SELLING' | 'ANALISTA_CONTEUDO' | 'GESTOR_TRAFEGO' | 'GESTOR_MARKETPLACE' | 'ADMINISTRATIVO' | 'CS';

// Em ROLE_LABELS_NEW, adicionar:
CS: 'CS',

// Em ALL_ROLES, adicionar:
'CS'

// Em ROLE_COLORS, adicionar:
CS: { bg: 'hsla(200,70%,45%,0.15)', text: 'hsl(200,70%,65%)', border: 'hsla(200,70%,45%,0.3)' },
```

- [ ] **Step 2: Commit**

```bash
git add src/types/roles.ts
git commit -m "feat: add CS to AppRole type and role maps"
```

---

### Task 3: Adicionar hook useIsCS

**Files:**
- Modify: `src/hooks/useUserRoles.ts`

- [ ] **Step 1: Adicionar hook após useIsSocialSelling**

```typescript
export function useIsCS() {
  const { data: userRole } = useCurrentUserRole();
  return userRole?.role === 'CS';
}
```

- [ ] **Step 2: Commit**

```bash
git add src/hooks/useUserRoles.ts
git commit -m "feat: add useIsCS hook"
```

---

### Task 4: Push e publicação

- [ ] **Step 1: Push para GitHub**

```bash
git push
```

Lovable sincroniza automaticamente após o push e publica a nova versão.
